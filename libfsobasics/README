====================================
freesmartphone.org framework library
====================================

This library contains utility functions for freesmartphone.org API implementors.
It is planned to release parts of this library seperately as C library.

Paradigm: Test Driven Development

Indentation: -bad -bap -bbb -bl -bli0 -bs -cbi4 -cli4 -i4 -l100 -lp
             -nbbo -nbc -ncs -npcs -npsl -nut 
             -pi0 -prs-saf -sai -saw -sbi4 -st

Note: This may need some Vala patches that have not been integrated into
an upstream release yet. We have a Vala mirror at freesmartphone.org that
can compile this library.

