libfsobasics:
 * add SmartKeyFile.pathValue( section, key, constraints, defaultpath ) querying for a path from the configuration,
 checking several whether the constraints are met; if not, return the default.







* Add unit tests for transport classes, including MockTransport

* Write netlink dispatcher for fsoframework

* Write libgpsprotocols as C library


==transport/glib==
Find out how to make sure that we always read what's available from
a transport before attempting to write.


==fsogsmd==
===modem_ti_calypso===
* do not open all channels concurrently; instead, open the first channel and wait until
  all the modem setup has been carried out. Only after that, open channels 2-4.

==fsotdld==
===alarm===
* Should we support a customizable object path for alarm notifications?
* Should registered alarms be persistent in case the daemon dies or the system gets rebooted?
===source_ntp===
* Listen for default route changes; then trigger sync
* Sync regularly (configurable frequency)

==fsousaged==
*Queue all commands

