serial-forward / pty-forward
============================
serial-forward allows you to forward a serial device node
(such as a modem) over a TCP network connection.

On the other end, you can use pty_forward to forward the
network device to a pseudo tty.


